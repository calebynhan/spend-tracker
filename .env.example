PORT=3001
HOST=127.0.0.1
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3001,http://127.0.0.1:3001

# Required when HOST is anything other than 127.0.0.1 or localhost.
# Generate your own value and do not commit it:
# openssl rand -base64 32
FLOW_ACCESS_KEY=
